# qt6ct handles the style via its own config (Breeze-Dark + bitzdots palette).
# QT_STYLE_OVERRIDE must NOT be set — it conflicts with qt6ct and can force
# the light Breeze style, breaking the dark theme.
QT_QPA_PLATFORMTHEME=qt6ct
